"""Backend test suite.

A package rather than a loose directory so that the shared fixtures and builders
in ``conftest`` can be imported by name from the subdirectories, instead of
being reached for through the import path.
"""
