"""Unit tests for the AI Pipeline.

A package rather than a loose directory so that the shared fixtures and stage
doubles in ``conftest`` can be imported by name instead of being reached for
through the import path.
"""
